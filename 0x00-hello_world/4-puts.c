#include<stdio.h>
#include<string.h>

/**
 * main - description
 *
 * Return: it returns a value.
 */
int main(void)
{
	char p[52];

	strcpy(p, "\"Programming is like building a multilingual puzzle");
	puts(p);
	return (0);
}
